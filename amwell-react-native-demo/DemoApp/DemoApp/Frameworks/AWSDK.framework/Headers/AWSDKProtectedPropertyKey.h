//
//  AWSDKProtectedPropertyKey.h
//  AWSDK
//
//  Created by Calvin Chestnut on 7/6/16.
//  Copyright © 2018 American Well.
//  All rights reserved.
//
//  It is illegal to use, reproduce or distribute
//  any part of this Intellectual Property without
//  prior written authorization from American Well.
//

#pragma mark Protected Property Keys

/**
 @name Protected Property Keys
 */

/**
 Key used to determine if user's address is feed controlled

 @since 2.0.0
 */
static NSString *kProtectedAddress = @"primaryAddress";

/**
 Key used to determine if user's first name is feed controlled

 @since 2.0.0
 */
static NSString *kProtectedFirstName = @"firstName";

/**
 Key used to determine if user's middle initial is feed controlled

 @since 2.0.0
 */
static NSString *kProtectedMiddleInitial = @"middleInitial";

/**
 Key used to determine if user's last name is feed controlled

 @since 2.0.0
 */

static NSString *kProtectedLastName = @"lastName";

/**
 Key used to determine if user's gender is feed controlled

 @since 2.0.0
 */
static NSString *kProtectedGender = @"gender";

/**
 Key used to determine if user's birth date is feed controlled

 @since 2.0.0
 */
static NSString *kProtectedDateOfBirth = @"dateOfBirth";
